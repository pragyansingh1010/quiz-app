# Question State

A question should have one explicit selected-answer state. Navigation must preserve answers already selected, and submission should calculate the result from the final stored answer set rather than from the visible question alone.